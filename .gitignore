# Environment (keep .env.example)
.env
.env.local
.env.*.local

# Firebase key (keep credentials out of git)
firebase-key.json
firebasekey.json
*-key.json

# Uploads
uploads/

# Dependencies
node_modules/

# Build / cache
dist/
.expo/
*.log
npm-debug.log*
